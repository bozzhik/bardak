{
  "name": "bot",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bunx --bun serve -l 2000 .",
    "start": "bun --watch src/index.ts",
    "build": "node -e \"process.exit(0)\"",
    "lint": "eslint --max-warnings 0",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@repo/shared": "*"
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "eslint": "^9.39.1",
    "typescript": "^5.9.2"
  }
}
